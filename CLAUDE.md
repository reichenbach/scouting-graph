# Rules for agents editing this repo

This repo is public bound. Everything in it is meant to be readable by a
stranger and runnable by a stranger.

## Never name

Do not add, and remove on sight: names of coaches, players, schools, teams,
conferences, clubs, employers, or any pitch tracking or video vendor. Call the
input "a pitch tracking export" and nothing more specific. Opponents are
"Opponent A". Pitchers are ids like "OPP-11". Roster names are tree names on
purpose.

Nothing from any employer, any client, or any other repo on this machine
belongs here. Not a file, not a schema, not a system name, not a screenshot.

## Synthetic data only

Every CSV in `sample_data/` and `inbox/` is generated by
`scripts/make_sample_data.py` with a fixed seed. If you need different data,
change the generator and regenerate. Never commit a real export, not even a
redacted one.

## Numbers come from code

`facts.py` computes every number. The model is handed a rendered facts sheet
and writes sentences about it.

- Do not let the model compute, estimate, average, or infer a number.
- Do not add a number to a report that does not exist as a fact with an id
  and an n.
- If you add a metric, add it in `facts.py`, give it a fact id, and give it
  a sample size. A number without an n does not go on a page.

`checks.py` is the enforcement, and it must stay strict. Loosening the
tolerance or adding an exception to make a draft pass is the one change that
defeats the whole repo. If a legitimate pattern trips the check, add a named
and documented exception with a test, the way the ball strike count notation
exception is written.

## Gates block

A gate returns failures. It never repairs, never imputes, never lowers a
threshold at runtime. A failure holds the whole report. There is no partial
delivery path, and none should be added.

Every gate failure carries two texts: `operator_detail` (loud and specific)
and `coach_note` (plain and kind). Both are required.

## A person approves

The `review` node is a real LangGraph `interrupt`. Do not add a bypass, an
auto approve flag, or an environment variable that skips it. Bench Coach
does not send messages, and no send path should be added to it.

## Naming

The assistant graph is called **Bench Coach**, two words, both capitalized.
The module keeps its filename, `assistant_graph.py`. The word AI is never part
of the name.

## Audit rows

Every terminal path writes an audit row, including holds. If you add a
terminal node, it writes a row, with the version stamp and the file
provenance.

Bump `PIPELINE_VERSION` in `config.py` whenever `facts.py` changes what a
number means.

## Tests

`make test` must stay green, offline, with no API key. Tests run with
`YDS_GRAPH_STUB=1` and a temporary `YDS_GRAPH_HOME`, so they never touch the
real outbox, errors folder, or audit database. New behaviour gets a test that
proves the failure path, not only the happy path.

## Secrets

Keys live in `.env`, which is gitignored. Never commit a key, never print one,
never add one to a sample file.

## Style

Plain sentences in docs and in coach facing strings. No emdashes. No marketing
voice. Say what the code does and what it refuses to do.
